# JSLIFE 作成レポート

## 目的

KodeLife のライブコーディング体験を参考に、ブラウザだけで JavaScript のビジュアル表現を編集・実行できる制作スタジオを構築した。

## 作成手順

1. Next.js / React / TypeScript ベースのブラウザアプリを初期化。
2. 画面を「ツールレール」「JavaScript エディター」「ライブプレビュー」「プリセット」の4領域に設計。
3. Canvas 2D の描画関数を実行するランタイムを実装。`ctx`、`canvas`、`time`、`pointer`、`frame` を各フレームへ渡す構成とした。
4. 編集後の自動実行、手動実行、再生・一時停止、リセット、FPS・経過時間表示を追加。
5. Neon Bloom / Liquid Grid / Solar Echo の3つのスターターパッチを作成。
6. 構文・実行時エラーをプレビュー上に表示し、コードはブラウザのローカルストレージへ保存するようにした。
7. PC では左右分割、タブレット・スマートフォンでは縦積みになるレスポンシブレイアウトを追加。
8. キーボード操作（Command/Ctrl + Enter で実行、Tab でインデント）とアクセシブルなラベルを追加。
9. 本番ビルドを実行して型・コンパイルを検証。

## 主な操作

- エディターへ JavaScript を入力すると、Auto-run が有効な場合は約 320ms 後に反映される。
- `Command + Enter`（Windows は `Ctrl + Enter`）で即時実行。
- 下部のカードで描画プリセットを切り替え。
- Canvas 上でポインターを動かすと、`pointer.x` / `pointer.y` を利用する作品へ反映。
- Run 実行時のコードは、このブラウザへローカル保存される。

## 実装上の判断

- 外部エディターや描画ライブラリに依存せず、Canvas 2D とブラウザ標準 API だけで動かした。
- 利用者が入力した JavaScript は同一ページ上で実行されるため、信頼できるコードを試す個人制作環境として設計している。公開サービスで第三者コードを扱う場合は、Worker / iframe sandbox などへの実行分離が必要。
- KodeLife の外観コピーではなく、暗色の制作ツール、ライム色のステータス、コンパクトなタイムラインという独自の JSLIFE デザインへ置き換えた。

## ファイル構成

- `app/Playground.tsx`: エディター、Canvas ランタイム、操作、プリセット
- `app/page.tsx`: スタジオ画面のエントリー
- `app/globals.css`: 全体デザインとレスポンシブ対応
- `app/layout.tsx`: ページ言語とメタデータ
- `REPORT.md`: 本レポート
